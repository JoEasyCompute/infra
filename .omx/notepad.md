

## WORKING MEMORY
[2026-04-09T08:17:00.198Z] Updated disktest.sh UX/docs: default TTY interactive flow, mode picker, disk picker, non-interactive override, per-disk reports, and timezone-aware report timestamps. Verified with bash -n, git diff --check, and Dockerized Linux/Bash5 runs for help, interactive dry-run, and health JSON/report output.

[2026-04-09T08:48:42.858Z] Enhanced disktest interactive disk picker to support arrow-key navigation with checkbox selection: up/down to move, space to toggle, enter to confirm, a to toggle all, q to cancel. Verified in Docker TTY that deselecting sda and confirming leaves only sdb selected in the dry-run plan.
[2026-04-09T09:06:57.638Z] Updated fulltest.sh with build/rebuild permission preflights: warn early if BUILD_DIR or cloned repos are root-owned/unwritable, and advise chown/sudo fixups before rebuild/reclone or --clean. Also updated docs/fulltest.md with a note about permission warnings and ownership recovery. Verified with bash -n, git diff --check, and temp-copy startup/clean warning checks.
[2026-04-09T09:12:23.145Z] Refined fulltest.sh permission handling: rebuild/clone/helper-write paths now check writability earlier and print ownership-fix guidance before failing. Verified with bash -n, git diff --check, and Docker bash5 non-root unwritable-build --clean run.
[2026-04-09T09:38:36.461Z] Proceeding on gpu-fulltest-v2 prototype: add dedicated docs, verify prepare-then-run behavior, then commit/push the experimental script separately from stable fulltest.sh.
[2026-04-09T09:40:22.969Z] Added docs/gpu-fulltest-v2.md, README linkage, and .gitignore entry for gpu_fulltest_v2 logs. Fresh verification: bash -n, diff check, help output, and dockerized mocked cuda-samples run showing prepare succeeds before execution in gpu-fulltest-v2.sh.
[2026-04-10T07:54:42.299Z] Created docs/project-decisions.md as a human-readable mirror of the current OMX project memory: stable vs experimental paths, current decisions, operator notes, and recent validation notes.
[2026-04-10T08:00:35.194Z] Checkpoint: repo clean on main@83eb673 (pushed). No pending file changes. Project-decisions doc added and memory refreshed.
[2026-04-10T08:01:16.060Z] Verification run on macOS host: fulltest.sh --help and gpu-fulltest-v2.sh --help both pass; disktest.sh runtime fails on /bin/bash 3.2 due declare -A (requires Bash 4+). Syntax check still passes.
[2026-04-10T13:50:42.035Z] Fresh fulltest pytorch evidence (2026-04-10): user traceback now shows multiple ranks segfaulting inside torch.distributed.utils._verify_param_shape_across_processes during DDP init. User paths indicate Python 3.12. Official PyTorch issue #125990 reports torchrun/c10d segfaults on Python 3.12, while current docs confirm the script's one-process-per-GPU DDP pattern is standard. Strong suspicion shifted toward Python 3.12 / PyTorch distributed runtime stack rather than script logic alone.
[2026-05-07T14:09:16Z] Added uv, stress-ng, fio, and lm-sensors to both base installers; uv is installed via Astral installer into /usr/local/bin and cleaned up on uninstall. Prototyped gpu-fulltest-v2 node-stress for concurrent CPU + RAM + GPU load, default 5 minutes, and committed/pushed as 9bfdebf before touching stable fulltest.sh.
[2026-05-07T15:02:53Z] Mirrored node-stress into stable fulltest.sh: concurrent stress-ng CPU + RAM load plus GPU burn, selectable with --node-stress-minutes (default 5), while keeping existing GPU-only stress unchanged. Updated docs/fulltest.md and refreshed the node-stress operator notes in project memory.
[2026-05-07T15:06:51Z] Added standalone test/cpu-ram-stress.sh to isolate host-only issues with stress-ng CPU + RAM load. Default runtime is 5 minutes, memory reserve and worker counts are tunable, and the script auto-installs stress-ng when run as root unless --no-install is set. Documented in docs/cpu-ram-stress.md and README.md.
[2026-05-16T11:15:06Z] Updated base-install.sh shell alias provisioning: repo .aliases is copied to ~/.aliases, bash/zsh source it via managed rc blocks, fish sources generated ~/.aliases.fish, and uninstall removes the managed alias files.
[2026-05-16T11:20:09Z] Added docs/cpu-test.md for test/cpu-test.sh: single-logical-CPU stress via stress-ng with socket0/socket1 modes, per-CPU timing, optional temperature logging, and local log/progress files.
[2026-05-16T11:31:50Z] cpu-test.sh now uses a per-run log directory under /tmp by default, with optional --run-dir override for reusing the same log/progress state; docs/cpu-test.md was updated accordingly.
[2026-05-16T14:58:29Z] base-install.sh now provisions the target user SSH authorized key and a passwordless sudoers drop-in when absent, alongside repo alias wiring and the existing base package/tooling setup.
[2026-05-16T14:58:55Z] base-install.sh now adds the repo SSH authorized key for the target user, creates a passwordless sudoers drop-in when absent, and removes both managed access artifacts during uninstall.
[2026-05-16T15:03:05Z] cpu-test.sh now writes stress_summary.txt in the per-run directory, updates it after each CPU, and flushes the run artifacts so a hang or power loss leaves a usable checkpoint.
[2026-05-19T07:24:50Z] cpu-test.sh now stresses one CPU/socket at a time using all local logical threads, stores reboot-survivable checkpoints under /var/tmp by default, supports --status for post-reboot inspection, and supports --reset-state to clear residue before retesting.
[2026-05-19T07:27:38Z] cpu-test.sh now supports both socket-level and per-thread targeting via --granularity socket|thread. Socket remains the default; thread restores the old one-logical-CPU-at-a-time mode with the same checkpoint and reset workflow.
[2026-05-19T07:42:27Z] cpu-test.sh now defaults to /var/tmp/cpu-test for state, supports --status and --reset-status without requiring --run-dir, and no longer exits after the first successful target because the pass/fail counter updates were made set -e safe.
[2026-05-19T07:51:23Z] cpu-test.sh default runtime is now 300 seconds per target instead of 60, with the existing socket/thread targeting and status/reset workflow unchanged.

[2026-05-20T17:49:43Z] Added optional RAID / ESP redundancy lane: install/install-raid.sh stages helper scripts by default, requires --activate for apt hook + systemd units, and keeps non-RAID hosts unaffected. Updated README, docs/project-decisions.md, install/raid/INSTALL.md, and refreshed project memory.

[2026-05-20T18:03:40Z] cpu-test.sh default runtime was increased to 300 seconds per target, and docs/cpu-test.md was updated to match. Verified with bash -n, --help, and --status; no active run state currently exists in /var/tmp/cpu-test.
